
import React, { Component } from 'react';
import { Actions } from 'react-native-router-flux';
import {
  StyleSheet,
  Text,
  TouchableHighlight,
  Dimensions
} from 'react-native';

const Loading = () => {
        return(
          <Text> Loading coachware </Text>
        )
  };
export default Loading;